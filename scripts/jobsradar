#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PROJECT_DIR=$(dirname -- "$SCRIPT_DIR")
. "$PROJECT_DIR/scripts/lib/jobsradar-runtime.sh"
jobsradar_configure_database_url

exec bun "$PROJECT_DIR/scripts/jobsradar.ts" "$@"
